import { SortExternalFunction } from './_.types'

export const sortExternalWrong: SortExternalFunction = (idToData: Record<string, number>, idToSeq: Record<string, number>): number[] => {
  return []
}
